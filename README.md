# Diagnostic Information System

Step 1 foundation for diagnostic workflow, data integrity, tenancy boundaries, and future interoperability adapters. It is not a PACS, LIS, EMR, device connection, or standards-conformant implementation.

Run `npm test` and `npm run lint`. All test data is synthetic.

See [PROJECT_STATUS.md](PROJECT_STATUS.md) for the implementation boundary and validation status.
