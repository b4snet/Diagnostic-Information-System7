# Provenance

Every Step 2 canonical factory requires provenance containing source type, source identifier, recorded time, optional correlation ID, and transformation list. Provenance flows into acquisition, observation, interpretation, verification, and protected object metadata. It is structural traceability only; integration import/export events and persistent audit linkage await a persistence adapter.
